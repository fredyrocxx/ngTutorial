import { SqaureOfNumberPipe } from './sqaure-of-number.pipe';

describe('SqaureOfNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SqaureOfNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
